version: 2

sources:
  - name: raw_data
    description: "Raw data sources for NYC taxi rides"
    database: kestra-sandbox-485712 # BiqQuery project ID
    schema: zoomcamp # name of database in the above project in BQ

    tables:
      - name: green_tripdata
      - name: yellow_tripdata